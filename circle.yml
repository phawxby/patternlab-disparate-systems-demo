machine:
  node:
    version: 6

dependencies:
  override:
    - npm install

database:
  override:
    - echo "Skip."

compile:
  override:
    - gulp patternlab:build

test:
  override:
    - echo "Skip."

deployment:
  release:
    branch: master
      commands:
        - gulp deploy:sftp